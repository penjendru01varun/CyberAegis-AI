# HyberShield AI Backend — Python Dependencies

# Core framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-core==2.18.2

# Rate limiting
slowapi==0.1.9

# Async HTTP (for future external integrations)
httpx==0.27.0

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1

# Optional: real DB drivers (uncomment when deploying with full stack)
# asyncpg==0.30.0          # PostgreSQL async driver
# motor==3.6.0             # MongoDB async driver
# neo4j==5.27.0            # Neo4j graph DB driver
